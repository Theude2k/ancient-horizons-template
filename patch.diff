```diff
diff --git a/src/components/Board.tsx b/src/components/Board.tsx
new file mode 100644
index 0000000..b1e2d3f
--- /dev/null
+++ b/src/components/Board.tsx
@@ -0,0 +1,37 @@
+import React, { useState } from 'react';
+
+const Board: React.FC = () => {
+  const [selected, setSelected] = useState<boolean[][]>(
+    Array.from({ length: 6 }, () => Array(6).fill(false))
+  );
+
+  const handleClick = (row: number, col: number) => {
+    const newSelected = selected.map((rowArr, rowIndex) =>
+      rowArr.map((cell, colIndex) =>
+        rowIndex === row && colIndex === col ? !cell : cell
+      )
+    );
+    setSelected(newSelected);
+  };
+
+  return (
+    <div className="grid grid-cols-6 gap-2">
+      {selected.map((row, rowIndex) =>
+        row.map((isSelected, colIndex) => (
+          <div
+            key={`${rowIndex}-${colIndex}`}
+            onClick={() => handleClick(rowIndex, colIndex)}
+            className={`w-16 h-16 border ${isSelected ? 'bg-blue-500' : 'bg-white'}`}
+          ></div>
+        ))
+      )}
+    </div>
+  );
+};
+
+export default Board;
diff --git a/src/index.tsx b/src/index.tsx
index d4f32de..8f34de4 100644
--- a/src/index.tsx
+++ b/src/index.tsx
@@ -1,5 +1,9 @@
 import React from 'react';
 import ReactDOM from 'react-dom/client';
+import './index.css'; // Assuming Tailwind is imported in index.css
+import Board from './components/Board';
 
 const root = ReactDOM.createRoot(
   document.getElementById('root') as HTMLElement
 );
 
-root.render(<React.StrictMode>/* App Components */</React.StrictMode>);
+root.render(
+  <React.StrictMode>
+    <Board />
+  </React.StrictMode>
+);
```
